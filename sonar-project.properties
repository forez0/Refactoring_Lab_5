# Основна інформація про проєкт
sonar.projectKey=library_service_key
sonar.projectName=Library Service
sonar.projectVersion=1.0

# Шляхи до вихідного коду
sonar.sources=.
sonar.sourceEncoding=UTF-8

# Налаштування для Python
sonar.language=py
sonar.python.version=3

# Налаштування тестів
sonar.tests=tests
sonar.test.inclusions=tests/**/*.py
sonar.test.exclusions=**/__pycache__/**,**/*.pyc

# Винятки
sonar.exclusions=**/__pycache__/**,**/*.pyc,**/.venv/**,**/venv/**

# Налаштування покриття коду
sonar.python.coverage.reportPaths=coverage.xml
sonar.python.xunit.reportPath=test-results.xml  # Якщо використовуєте xUnit

# Налаштування SCM
sonar.scm.provider=git
sonar.scm.disabled=false  # Краще увімкнути для аналізу

# Додаткові налаштування
sonar.verbose=true